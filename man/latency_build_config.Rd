% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/latency_config.R
\name{latency_build_config}
\alias{latency_build_config}
\title{Build a latency config from a campaign id and its CSV}
\usage{
latency_build_config(
  campaign_id,
  data,
  field_timezone = "UTC",
  project_id = NULL,
  texting_windows = list(),
  date_filter = NULL,
  respondent_id_column = NULL
)
}
\arguments{
\item{campaign_id}{Campaign id (numeric or character).}

\item{data}{A data frame of CSV results (or a character vector of column
names) used to discover the question flow.}

\item{field_timezone}{Tz used to bucket the Parquet \code{date} and
\code{hour_local} columns. Default \code{"UTC"}.}

\item{project_id}{Optional Survey160 project id; defaults to the
campaign id as a placeholder.}

\item{texting_windows}{Optional list of \code{{date, start_hour, end_hour}}
windows. Default \code{list()} = all-in-window.}

\item{date_filter}{Optional character/Date vector restricting which
survey dates are processed (interpreted in \code{field_timezone}).}

\item{respondent_id_column}{Optional column name used to dedupe rows by
respondent. Default \code{NULL} (no dedupe).}
}
\value{
A validated config list ready to pass to \code{latency_report()}.
}
\description{
Pure function. Derives \code{flow.questions} from the CSV column names
via \code{latency_discover_questions()} and assembles the rest of the
config from the named arguments. No I/O, no API call, no auth precondition.
}
